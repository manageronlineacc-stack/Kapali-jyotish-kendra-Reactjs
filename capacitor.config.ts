import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kapalijyotishkendra.app',
  appName: 'kapali-jyotish-kendra-app',
  webDir: 'build'
};

export default config;
